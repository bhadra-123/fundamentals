# Gain Root Access, you will be in ~
sudo su -

# Gain Root Access, you will be in /home/azureuser/
sudo su

# To repeat the previous command(most recent)
!!

# Enter /etc/ but remembers the old path you are working on
pushd /etc/

# Will take you back from /etc/ to old path you previously working on
popd

# To Clear the Screen
clear

# To Clear the Screen, Scroll up to see the history of Commands
ctrl + l

# Prints Good Morning
echo "Good Morning"

# Append ls command at the end of the sample.txt file
echo "ls" >> sample.txt

# Replace the content of sample.txt with ls
echo "ls" > sample.txt

# Look at OS type of Servers
cat /etc/os-release

# Creating Directories
mkdir dir1 dir2 (Creating directories)
mkdir dir1/subdir-1 (Creating sub-directories after creating main directory)
mkdir dir2/subdir-2 (Creating sub-directories after creating main directory)
mkdir -p dir3/sub-dir3 dir4/sub-dir4 (Creating sub-directory without making main directory by making it as parent using -p)

# Creating Directories and files without creating main Directory:
mkdir -p dir1/subdir-1 dir2/subdir-2

# Creating files inside directories without going inside the directories
touch dir1/sub1/file1.txt dir1/sub1/file1.pdf dir2/sub2/file2.txt dir2/sub2/file2.pdf












    